# Deny direct access to uploaded files for security
# Only allow access through PHP scripts that can validate permissions

# Deny all direct access
Deny from all

# Allow only specific image types through PHP
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    # Still deny direct access - files should be served through PHP
    Deny from all
</FilesMatch>
